# Knowledge Pipeline

**Purpose:** Learn once, verify, store, and reuse.

Flow: ingest → identify source → detect duplicates → validate source → research → verify facts → extract → classify → generate metadata/Markdown → link → review → approve → index.

Raw inputs remain in `09_Inbox`. Approval is a human-accountable action. Runtime task memory is never promoted automatically.
